// import {useContext}
import './Showcase.scss';

const Showcase = props => {
    return (
        <section id='main-showcase'>
            SHOWCASE
        </section>
    )
}

export default Showcase;